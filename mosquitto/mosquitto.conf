# Mosquitto broker config — local dev only.
# Anonymous access is fine here (single-user, localhost); production would add
# auth + TLS. Sensor nodes publish to superintendent/sensors/<node_id>/<band>.
listener 1883
allow_anonymous true
